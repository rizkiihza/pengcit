<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.martin.tugas2_pengcit">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HistogramActivity" />
        <activity android:name=".TransformationAcitivty" />
        <activity android:name=".SpecificationActivity" />
        <activity android:name=".ChainActivity" />
        <activity android:name=".asciiActivity" />
        <activity android:name=".Convolution" />
        <activity android:name=".FaceRecognition" />
        <activity android:name=".FFTActivity" />
        <activity android:name=".UASActivity"></activity>
    </application>

</manifest>